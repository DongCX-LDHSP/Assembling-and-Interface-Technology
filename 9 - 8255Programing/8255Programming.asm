DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
INIT:
    MOV AL, 90H; 即10010000B
    MOV DX, 646H
    OUT DX, AL

S:
	MOV DX, 640H
	IN AL, DX
	MOV DX, 642H
    OUT DX, AL
	JMP S
    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
